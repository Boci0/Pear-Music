<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="ic_launcher_background">#FFFFFF</color>
    <!-- The app is always dark: the launch window and the window behind the
         Flutter UI use the same near-black as the scaffold, so there is no
         white flash on light-mode devices. -->
    <color name="pear_background">#0C0C0E</color>
</resources>
